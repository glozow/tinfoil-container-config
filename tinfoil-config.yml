# Guard-shaped deploy config on the `guard` branch.

cvm-version: 0.7.3
cpus: 2
memory: 8192

containers:
  - name: "guard"
    image: "ghcr.io/glozow/guard:v0.0.2@sha256:952928b3079fb3daa677e6f5d6a161d08f3f090662cb1f3e96ded72541a50e49"

shim:
  upstream-port: 8080
  paths:
    - /classify
    - /pubkey
    - /health
