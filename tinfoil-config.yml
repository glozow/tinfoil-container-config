# Gated-inference shape on the `gated-inference` branch.

cvm-version: 0.7.3
cpus: 4
memory: 16384

containers:
  - name: "gated-inference"
    image: "ghcr.io/glozow/gated-inference:v0.0.2@sha256:57aea7a36b5ce9ffba30caf665469f8e4354af30dade06db9b77be7abf999685"
    env:
      - GUARD_ENCLAVE: "guard-2-llamaguard31bq4.girizen.containers.tinfoil.dev"
      - GUARD_REPO: "glozow/tinfoil-container-config"

shim:
  upstream-port: 8080
  paths:
    - /generate
    - /pubkey
    - /health
